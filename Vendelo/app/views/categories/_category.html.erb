<div id="<%= dom_id category %>">

    <strong>Name:</strong>
    <%= category.name %>
     <%= link_to t(".edit"), edit_category_path(category) %>
     <%= link_to t(".destroy"), categories_path(category), data:{
      turbo_method: :delete ,
      turbo_confirm: t("common.confirm")
     } %>
  

</div>
