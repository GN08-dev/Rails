<h2> <%= t(".title") %> </h2>

<h2 class="title" ><%= @product.title %></h2>
<%# la condicional de la imagen es para saber si tiene una imagen o no %>
<%= image_tag @product.photo, width: 200 if @product.photo.attached? %>
<h3>El costo del producto es: <%= @product.price %></h3>
<p>Descripción:</p>
<p><%= @product.descripcion %></p>

<strong>  <%= link_to "Editar producto", edit_product_path(@product) %>  </strong>
 <%= link_to "Eliminar producto", product_path(@product), data:{
    turbo_method: :delete,
    # para confirmar si se decea eliminar o no este es un modal pequeño de turbo pero si no se desea
    #se puede omitir 
    turbo_confirm: t("common.confirm")
} %> 